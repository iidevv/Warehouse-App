import ChannelContainer from "../components/Catalog/ChannelContainer";

const Channel = () => {
  return (
    <div className="container">
      <ChannelContainer />
    </div>
  );
};

export default Channel;
